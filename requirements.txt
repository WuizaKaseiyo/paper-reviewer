# This is a pure-skill talent — no Python dependencies are needed.
# The talent runs inside OMC's LangChain agent using only stock base tools
# (read, ls, glob_files, grep_search, write, bash). No external libraries
# to install when the talent is hired.
